#include "logic.h"

int main() {
	
	cout << "is perfect number ? " <<
		(is_perfect_number(6) ? "yes" : "no") << endl;
	cout << "is perfect number ? " <<
		(is_perfect_number(8) ? "yes" : "no");



	return 0;
}